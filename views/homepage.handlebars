{{#each posts as |blogPostData| }} 
<div class="blogpost align-center mb-5">
  <div class="col-md-6">
    <h2>{{blogPostData.title}}</h2>
    <p>{{blogPostData.body}}</p>
    <h3>{{blogPostData.username}}</h3>
  </div>
  <div class="col-md-6">
    <h3><a></a></h3>
  </div>
</div>
{{/each}}